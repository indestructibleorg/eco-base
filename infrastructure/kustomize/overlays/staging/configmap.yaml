# Intentionally left without resources.
# The previously defined `ecosystem-config` ConfigMap was unused (no deployments
# referenced it for env/envFrom), so it was removed to avoid dead configuration.